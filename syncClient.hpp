#include "stdafx.h"
/*#include <iostream>
#include <WinSock2.h>
#include <WS2tcpip.h>

class SyncClient
{
private:

public:
	SyncClient();
	~SyncClient();

	SOCKET server;

	void connectClient();

	void sendClient(std::string message);

};*/